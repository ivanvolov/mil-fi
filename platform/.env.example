# Superseded — the full template now lives at the REPO ROOT: `.env.example`.
# Copy that one to the repo root as `.env` (the one shared env file for all
# apps in this repo). Real environment variables always win over it.
#
# Secrets are canonically stored in Infisical (EU): project HOC
# (1c02be0d-3fed-4e98-aa38-d02f4eb3a03b), env=dev, folder=/mil-fi.
# In production Render injects them at boot via `npm run start:infisical` —
# the only secret set on Render is INFISICAL_TOKEN (see render.yaml).
