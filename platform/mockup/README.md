# mockup/ — static reference

Two static HTML files (`screen-1.html`, `screen-2.html`) that sketch the tactical-map UX. Kept as a reference and for the JSON escape-hatch button on `screen-1.html` (top-strip, next to the copy-positions icon): it dumps `{ launchers, crews, threats, noFlyCoords }` as a downloadable file — handy if the live app ever regresses on data shape.
