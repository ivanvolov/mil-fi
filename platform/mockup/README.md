# mockup/ — frozen legacy

These two static HTML files (`screen-1.html`, `screen-2.html`) predate the app. They were the printable partner-deliverable spec that seeded the tactical-map UX. **They are frozen — no edits.**

Kept because `screen-1.html` still ships an occasionally-useful JSON escape-hatch button (top-strip, next to the copy-positions icon): it dumps `{ launchers, crews, threats, noFlyCoords }` as a downloadable file — handy if the live app ever regresses on data shape.

The original spec that produced these screens now lives at `/Users/ivanvolovik/p🗂️/Carcas/projects/droneBox/research/HOC_source_data/00_original_mockup_spec.md`.
