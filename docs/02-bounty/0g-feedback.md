# 0G Feedback — Compute Router Testing Documentation

Project: **MilFi** — defense-tasking platform where an autonomous payout only fires after a
0G-hosted vision model, running in TEE trust mode, independently confirms the threat/kill
verdict from the report and strike-footage images.

## Developer feedback

The OpenAI-compatible router made this the easiest integration in the whole stack — we had a
real vision-inference call running against a live 0G Compute model in under five minutes, no
SDK or custom auth flow to learn.
